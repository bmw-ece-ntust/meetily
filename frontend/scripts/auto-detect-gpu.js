#!/usr/bin/env node
/**
 * GPU detection disabled - transcription now handled by ai-meeting-agent API
 * This script is kept for backward compatibility but returns no features
 */

// No GPU features needed - all transcription is API-backed
console.error('ℹ️  Transcription handled by ai-meeting-agent API (no local GPU needed)');
process.exit(0);
